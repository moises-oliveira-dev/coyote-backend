services:
  database:
    image: postgres:17
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DATABASE: coyote
    ports:
      - "5432:5432"
    volumes: 
      - data:/var/lib/postgresql/data

volumes:
  data: 
